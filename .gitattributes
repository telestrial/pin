# Normalize all text files to LF in the working tree, regardless of a
# contributor's core.autocrlf setting. The repo is already all-LF in the
# index; this keeps the working tree matching so biome (which emits LF)
# stops flagging the handful of files that drifted to CRLF on checkout.
* text=auto eol=lf
